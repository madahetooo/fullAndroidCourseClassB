<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apps.fullcourseandroidclassb">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logocar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logocar"
        android:supportsRtl="true"
        android:theme="@style/Theme.FullCourseAndroidClassB">
        <activity android:name=".BullsCarsActivity"></activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".TodoListApplication" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ResturantMenu" />
        <activity android:name=".ImageViewExample" />
        <activity android:name=".Calculator" />
        <activity android:name=".Counter" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>